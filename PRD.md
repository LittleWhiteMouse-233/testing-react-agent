# Android TV 主观测试 Agent 产品需求文档（PRD）

> 版本：2.0
> 状态：团队目标对齐基线  
> 更新日期：2026-09-08

## 本轮架构决策（执行协议 v2）

本轮将外部能力统一为 MCP 工具，以下为当前产品约束。

- 宿主仅通过 mcp.json 导入工具，不管理设备实体、设备状态或设备配置；Planning 不访问 MCP。
- Act 达成目标；Judge 可准备条件、触发交互并验证行为，但不得绕过待验证行为或修改成功标准。两者使用同一 execution 模型及全部已启用工具。
- 截图由模型自主请求；MCP content 顺序表达操作与观察顺序。宿主校验证据已保存且属于当前任务，模型判断是否需要补拍。
- 每次模型回复消耗一轮（传输重试除外）。耗尽时有证据则 FAILED，无证据则 BLOCKED。
- State 保留 Message 身份、顺序、文字和工具调用关系，只保留最近 N 个产生截图的已完成决策轮（默认 N=3）的全部图片 base64；无图轮不占窗口。工具完成后同步将窗口外图片在原内容位置替换为格式化占位说明及 artifact 引用，任务终止时不额外清空窗口内图片。模型请求基于回收后的 State 执行 token 预算和必要上下文检查；token 裁剪只影响请求。持久事件与原图保留，开发人员可通过引用重建截图记录，历史不可覆盖。
- 独立 ATV MCP 拥有 YAML 配置、探活、队列校验和操作后等待。它是可信外部服务，不参与宿主测试，也不新增 ATV 专属测试。
- 宿主闭环测试仅通过独立通用 Fake MCP 的 stdio 协议完成，不依赖 ATV 或设备模型。仍保持人工确认不可变计划、单运行串行与全局 fail-fast。

## 1. 文档定位

本文档定义产品目的、范围和验收标准；当前技术边界以代码、测试及 README 为准，旧设计文档仅作历史参考。

提炼依据：

- `spec3.md`：当前产品与运行语义的主要依据；
- `.dev/project-analysis.html`：首版项目结构、实际闭环和能力覆盖情况；
- `review.first-build.md`：首版实现的架构审查与修正意见。

审查记录中关于状态图拆分、消息管理和 Function Calling 的意见属于实现设计问题，不直接构成产品功能。本 PRD 只吸收其产品层含义：Agent 在多轮执行中应保持上下文连贯，规划、操作和判定的职责应清晰，新增观察与操作能力不应改变核心产品流程。

## 2. 产品概述

### 2.1 一句话定义

Android TV 主观测试 Agent 是一套内部测试验证工具：测试人员用自然语言描述测试用例，系统将其转化为可人工确认的语义任务，并根据电视实时画面自主操作、判定、留存证据，最终生成可追溯的测试结果。

### 2.2 产品愿景

让无法仅靠固定脚本可靠完成的 Android TV UI、交互流程和主观体验测试，具备低门槛、可复核、可重复的自动执行能力，同时让人始终掌握执行前确认权和执行中的终止权。

### 2.3 要解决的问题

- 自然语言用例需要人工翻译为大量脆弱的按键脚本，维护成本高；
- TV 页面、焦点位置和弹窗可能变化，固定坐标或固定按键序列适应性差；
- 文本是否正常显示、页面是否可用等主观要求难以由传统断言覆盖；
- 测试过程中的操作、判定和截图分散，失败后难以复盘；
- AI 的判断存在不确定性，若缺少人工确认、执行边界和证据约束，结果不可信。

## 3. 目标用户与核心场景

### 3.1 主要用户

- Android TV 测试人员：创建用例、确认计划、发起执行、查看证据和报告；
- 测试开发人员：维护可复现用例，使用模拟或回放环境做回归验证；
- 设备或能力集成开发者：接入新的 TV 控制方式、观察能力或外部测试工具。

当前阶段面向受控的内部开发验证环境，不面向普通终端用户，也不建设复杂的组织、权限和审批体系。

### 3.2 核心使用场景

1. 测试人员输入“进入设置并检查关于页面中的固件版本号是否正常显示”；
2. 系统生成“进入设置页”“进入关于页”“检查固件版本号”等语义任务；
3. 测试人员查看前置假设，编辑任务及判定标准；每次编辑都保存为新的 immutable TestPlan；
4. 测试人员在 latest TestPlan 上点击启动并确认 assumptions，系统连接配置的 MCP 服务，自主请求截图并选择工具，直至任务完成、失败、阻塞或达到循环上限；
5. 测试人员实时查看当前截图、操作和判定，必要时取消运行；
6. 系统生成包含计划、过程、结论和截图证据的历史报告与导出文件。

## 4. 产品目标

### G1. 打通自然语言到测试报告的完整闭环

用户不需要先编写逐键脚本，即可完成用例录入、计划确认、设备执行、结果查看和报告导出。

### G2. 以语义目标驱动自适应操作

任务描述“要达到或验证什么状态”，Agent 根据当前画面决定下一步动作，而不是由规划阶段生成固定按键序列。

### G3. 让结果可信、可解释、可复核

每次观察、动作和判定都可追溯；最终判定引用真实截图；最终用例结果由固定规则聚合，不能由模型任意改写。

### G4. 保留必要的人类控制

计划必须在执行前可查看和修改；无法自动保证的前置条件必须由用户确认；运行过程中可以取消。

### G5. 支持重复执行和回归对比

计划版本与每次运行均不可变保存。重跑创建新的运行，不覆盖历史事实，并可在模拟、回放和真实设备环境中验证。

### G6. 为后续能力扩展保持稳定入口

新增设备、截图方式、组合操作或外部工具时，应延续同一套任务、证据、状态和报告语义，不迫使用户改变核心工作流。

## 5. 产品原则

1. **先确认，后执行**：模型生成的计划不是最终事实；用户查看 latest TestPlan 后点击启动即完成本次确认，历史版本只读且不能创建新运行。
2. **语义任务优先**：计划定义目标和成功标准，不预先固化具体按键路径。
3. **证据优先**：测试结论必须能回到对应观察和截图，而不是只保留模型文本。
4. **确定性结果**：模型负责观察和建议，程序规则负责状态流转和最终结果聚合。
5. **历史不可覆盖**：计划修改、重新执行和重复导出均产生新记录。
6. **执行必须有界**：任何任务都必须能在取消、异常或循环上限下结束，不能无限运行。
7. **明确区分失败与不可判断**：产品缺陷记为失败，设备或模型不可用记为阻塞，用户终止记为取消。
8. **先验证价值，再扩展平台**：优先证明核心闭环有效，不提前建设通用调度、插件市场或长期记忆平台。

## 6. 核心产品流程

```text
录入自然语言用例
  → 生成语义任务计划
  → 查看前置条件并编辑计划（编辑即创建新版本）
  → 在 latest TestPlan 上确认并启动
  → 连接配置的 MCP 服务并创建运行快照
  → 按任务循环“观察—决策—操作/判定”
  → 聚合 PASS / FAIL / BLOCKED / CANCELLED
  → 查看历史、证据和报告
  → 导出或创建新的重跑
```

## 7. 功能需求

### 7.1 P0：核心执行闭环

#### FR-01 用例管理

- 用户可以创建和查看自然语言测试用例；
- 用例至少包含名称和正文，正文可描述前置条件、操作目标和预期结果；
- 一个用例可以拥有多个计划版本和多次运行记录。

#### FR-02 计划生成

- 系统应将用例转换为有序的语义任务列表；
- 任务分为：
  - `Act`：通过可用工具达成可验证目标；
  - `Judge`：可以准备条件、触发交互并改变状态以验证行为，不得绕过待验证行为或修改成功标准；
- 每个任务必须包含清晰目标、一个或多个可验证的成功标准和最大循环次数；
- 计划不得用预设按键序列替代语义目标；
- 生成结果不合法时，系统应明确告知规划失败，不得从不可验证的自由文本中猜测可执行内容。

#### FR-03 前置条件确认与计划编辑

- 系统应区分：
  - 可由用户事先准备的提示步骤；
  - 无法自动验证、必须由用户确认的前置假设；
- 启动命令必须显式提交 `assumptions_confirmed=true`；成功创建 TestRun 及其时间就是确认事实，不另建确认状态，也不复制 assumptions 文本；
- 用户可以新增、编辑、删除和排序任务，也可以调整成功标准和循环上限；
- 提示步骤不会被系统静默转换为可执行任务。用户需要补充目标与成功标准后，才能将其加入执行计划；
- 每次保存修改都创建新的 TestPlan 版本并生成全新的任务 ID，不覆盖旧版本；
- 只有 latest TestPlan 可以继续人工修订或创建 TestRun；历史 TestPlan 仅用于解释历史运行和产品回溯，不能被选择、重跑或继续派生运行。

#### FR-04 运行创建与环境检查

- 一次运行固定绑定一个不可变 TestPlan，并冻结实际发现的 MCP 工具目录；用例由 TestPlan 关系唯一恢复；
- RunService 必须在创建事务中再次验证 TestPlan 仍为对应 TestCase 的 latest，运行创建成功后，后续新计划不得改变该运行的解释；
- TestRunSnapshot 只固化最终工具目录、execution 模型、两类 prompt 版本、截图窗口轮数、应用版本与执行协议版本；不保存设备实体或重复的计划/用例事实；
- 执行前连接配置的 MCP 服务并发现工具，宿主不进行设备探活；
- MCP 连接或发现失败时拒绝启动并返回可诊断错误；执行中的工具/模型不可用应按有限错误处理结束为 `BLOCKED`；
- 当前验证版本同一时刻只执行一个 TestRun，同一运行内的任务按计划顺序串行执行。

#### FR-05 自适应任务执行

- Agent 应围绕当前任务重复执行“获取最新画面—理解状态—选择动作或给出判定”；
- 每一步选择必须以当前画面、当前任务、成功标准、工具能力和最近执行上下文为依据；
- `Act` 任务在达到目标状态后通过；发现明确错误或目标不可达时可提前失败；
- `Judge` 可调用与 Act 相同的全部启用工具来验证原成功标准；操作成功本身不等于断言成立；
- 工具可用性仅由本次运行的 MCP 配置及发现结果决定，不再区分 Act/Judge scopes；
- 操作完成不等同于任务完成，任务仍需依据后续观察确认；
- 每次模型回复消耗一个决策轮，包括能力查询、观察、纠错和结束请求；模型传输重试不另计。耗尽轮数时有截图则 `failed`，无截图则 `blocked`；
- 截图、模型或外部能力在有限重试后仍不可用时，任务必须结束为 `blocked`，不得伪造业务结论；
- 对结果未知的操作不得盲目重复，系统应先重新观察设备状态。

#### FR-06 状态与结果规则

任务状态必须遵循以下语义：

| 状态 | 产品含义 |
|---|---|
| `passed` | Act 已达到目标，或 Judge 的成功标准成立 |
| `failed` | 目标明确未达到、断言不成立、出现业务异常，或达到循环上限 |
| `blocked` | 因设备、截图、模型或外部能力不可用，无法形成业务结论 |
| `skipped` | 因前序任务失败或阻塞，按规则未继续执行 |
| `cancelled` | TaskRun 已启动后在安全边界响应用户取消；不要求通过/失败截图证据，未启动任务不创建 TaskRun |

用例结果必须由固定规则计算：

| 条件 | 用例结果 |
|---|---|
| 任一已执行任务为 `failed` | `FAIL` |
| 无失败，但存在 `blocked` 或由其产生的 `skipped` | `BLOCKED` |
| 所有任务均为 `passed` | `PASS` |
| 用户取消运行 | `CANCELLED` |

当前版本采用全局 fail-fast：任一任务失败或阻塞后，剩余任务统一标记为 `skipped`，并结束运行。

TestRun 的完成边界是执行结果及终态成功持久化，随后关闭运行级 MCP 会话属于应用资源收尾。执行完成后的会话清理失败仅记录应用日志，不改变 verdict、不追加运行错误事件；执行中影响工具调用、取证或继续执行的会话故障仍按执行失败规则处理。资源清理结束前继续保持单运行占用，即使 TestRun 已完成也不接受下一次启动。已完成运行不再接受取消，清理期间的取消或应用关闭不得改写既有结果。

#### FR-07 证据与可追溯性

- Graph State 的 Message 历史承载观察、模型决策和工具结果的运行事实，允许按视觉窗口将过期图片转换为 artifact 引用；事件流以 `message.appended` 保存 Message 的稳定公开投影，图片回收前后公开投影相同，不重复追加或覆盖历史。持久事件与 artifact 原图共同保留完整历史证据；事件流只补充 Message 之外的生命周期、校验和错误事实；
- 每个通过或失败判定至少关联一张本次运行中实际保存的截图；
- 判定说明应明确关联“实际观察”与“成功标准”，但不要求展示模型完整思维过程；
- 运行事实只追加、不覆盖，页面、历史和报告应使用同一事实来源；
- 页面刷新或实时连接中断后，用户仍能恢复全部已保存的过程记录，不应因实时推送中断而丢失事实。
- 当前产品不要求显示模型 token/chunk 流；实时页面只消费已经形成的持久化运行事实。

### 7.2 P1：必要产品能力

#### FR-08 实时执行查看与控制

- 用户可以查看运行状态、当前任务、当前循环数、最新截图和按时间追加的事件摘要；
- 用户可以请求取消运行；取消应在当前单次操作结束后的安全边界生效；
- 取消必须得到 `CANCELLED`，不得被记为产品失败；
- 页面刷新后应能继续查看同一运行，已保存事件不得丢失或乱序。

#### FR-09 历史与报告

- 用户可以按用例查看计划版本和历史运行，并进入某次运行的详情；
- 报告应包含用例、不可变计划版本、前置假设、工具目录、模型上下文、最终结果、任务结果、Message-first 时间线、成功标准、错误和截图证据；
- 程序生成的状态和统计是报告事实，模型生成的原因分析或排查建议必须标明为分析性内容，不能覆盖事实结论。

#### FR-10 导出

- 当前版本支持 JSON 和 HTML 报告导出；
- JSON 应包含完整结构化运行数据和证据清单；
- HTML 应可离线查看主要结论和截图证据；
- 重复导出创建新文件，不覆盖旧导出。

#### FR-11 可测试运行环境

- 宿主通过通用 Fake MCP 场景与响应回放验证，测试不导入或启动 ATV；独立 ATV 服务视为可信外部实现，不新增专属测试；
- 自动化测试不得依赖真实电视、外部网络或真实模型凭据；
- 同一回放场景应可用于比较提示、模型或执行策略变化前后的结果、循环次数和错误类型。

### 7.3 P2：验证后扩展

以下能力只在核心闭环被验证后建设：

- 进程中断后的自动恢复和运行重试；
- PDF 导出、流程节点可视化、日志筛选和运行对比；
- 可独立扩展的观察与操作能力，例如局部截图、图像比对或组合操作；
- 静态 Skills；MCP 工具接入已纳入当前范围；
- 经效果评估证明有价值后，再增加 OCR、专用视觉模型或只读历史经验检索。

这些扩展必须沿用现有任务状态、证据记录、结果聚合和运行快照规则。

## 8. 非功能需求

### NFR-01 可靠性

- 所有任务均有循环上限、超时或取消路径；
- 暂时性异常只允许有限重试；
- 实时界面异常不得改变已经保存的运行事实；
- 恢复执行时不得自动重放结果未知的上一次动作。

### NFR-02 一致性与可复现性

- 计划版本和运行快照一经创建不可修改；
- 重跑必须创建新的运行；
- 实时页面、历史详情、在线报告与导出对同一运行给出一致结论；
- 重连或恢复不得产生重复事件或重复证据事实。

### NFR-03 可解释性

- 用户能够从最终结果逐层追溯到任务、判定说明、观察事件和截图；
- 系统必须显示失败、阻塞、跳过和取消的不同原因；
- 不保存或依赖模型完整思维链作为产品功能。

### NFR-04 安全与隐私

- API 密钥、设备凭据、运行数据库、截图和导出文件不得进入源代码仓库；
- 只有当前运行通过 MCP 明确启用并发现的工具可以被 Agent 使用；
- 外部模型需要接收截图时，应仅发送完成当前任务所需的上下文，并使用项目明确配置的模型服务。

### NFR-05 可扩展性

- 设备控制、模型服务和外部工具可替换或新增，而不改变核心用户流程；
- 新增能力通过 MCP 声明工具用途、参数和标准 annotations；宿主不增加设备专属模型或权限映射；
- Agent 在连续多轮操作中应保持必要上下文，避免把每一步当成彼此无关的孤立判断。

## 9. 当前范围与非目标

### 9.1 当前交付范围

当前产品基线覆盖单运行串行的计划—执行—证据—报告闭环，工具来自配置的独立 MCP 服务；宿主不负责设备资源管理。

### 9.2 当前非目标

- 多设备资源调度、多运行并发、分布式任务队列和高可用部署；
- 通用 DAG、任务优先级、动态插队和运行中修改计划；
- 自定义视觉模型训练、自动学习和未经评估的长期记忆；
- Skills 热加载、依赖自动安装、插件市场和完整 MCP 管理平台；
- 自研权限、审批或额外安全沙盒；
- 面向生产规模的对象存储和数据库集群；
- 像素级复刻特定产品界面；
- 用 AI 生成的解释替代确定性的测试结果。

## 10. 验收标准

产品基线满足以下条件时，可认为核心目标达成：

1. 输入标准自然语言用例后，能生成至少一个 `Act` 和一个 `Judge` 任务；
2. 用户能查看并确认前置假设、修改计划并保存为新 TestPlan；旧版本保持不变且不能启动，只有 latest TestPlan 可创建运行；
3. 模拟或回放环境能完整产生 `PASS`、`FAIL`、`BLOCKED` 和 `CANCELLED` 四类结果；
4. 无 ATV 安装或真实设备时，通用 MCP 场景仍能完成工具发现、文本/图片结果处理和完整运行闭环；
5. 每个通过或失败判定均至少关联一张有效证据截图；
6. 达到循环上限、设备离线或模型输出持续无效时，运行能按规则终止，不会无限执行或伪造结果；
7. 前序任务失败或阻塞后，剩余任务按全局 fail-fast 规则跳过；
8. 用户能在执行中取消，并获得与业务失败不同的取消结果；
9. 页面刷新或实时连接中断后，能恢复全部已保存事件并继续查看；
10. 在线报告、JSON 和 HTML 导出中的任务、证据、统计和最终结论保持一致；
11. 从当前 latest TestPlan 再次执行、计划修改和重复导出都创建新记录，不覆盖历史；
12. 自动化回归测试无需网络、真实模型凭据或真实电视即可运行。

## 11. 已知产品取舍与风险

### 11.1 模型判定误差

当前判定默认可查看最近 3 个有截图的已完成决策轮的全部图片；这些轮次可不连续，同轮多图共占一轮，后续无图回复不会使最新截图过期。视觉窗口同步回收 State 中过期图片的 base64，保留原位置的格式化占位说明和 artifact 引用；持久事件及原图可用于重建截图记录。窗口限制截图轮数，不是图片字节总量，也不放宽模型 token 预算。截图是否足以证明当前结果由模型判断；程序强制校验证据存在和任务归属，并确定性聚合结果。

### 11.2 全局 fail-fast 的证据损失

第一个失败或阻塞会停止后续任务，可能遗漏同一用例中其他独立检查点的证据。当前优先保证行为简单、成本可控和结果可预测；是否支持非阻断检查，应由实际验证数据决定。

### 11.3 前置步骤不自动执行

提示步骤不会自动变成任务，可避免执行缺少成功标准的模糊动作，但会增加少量人工编辑成本。

### 11.4 单运行串行限制

当前版本无法同时执行多个 TestRun。这是验证阶段的明确边界，不应在用户界面或接口层暗示已经支持并发调度。

### 11.5 主观要求的稳定性

“正常显示”“体验良好”等描述可能因模型、设备和画面差异产生波动。用例应尽量把主观描述细化为可从画面验证的具体标准，并通过回放与真实设备样例持续评估。

## 12. 团队共同决策基线

开发过程中遇到范围或实现争议时，按以下顺序判断：

1. 是否直接服务于“自然语言用例 → 人工确认计划 → 自适应执行与判定 → 证据化报告”的核心闭环；
2. 是否保持任务、状态、证据和最终结果的明确语义；
3. 是否增强可解释、可复现和人工可控，而不是仅增加 Agent 的自由度；
4. 是否属于当前 P0/P1 范围；若仅为未来扩展，应保持兼容但不提前建设；
5. 若产品语义需要改变，应先更新本 PRD 和验收标准，再修改技术设计与实现。
